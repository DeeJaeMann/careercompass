import logging

info_logger = logging.getLogger("django_info")
error_logger = logging.getLogger("django_error")
debug_logger = logging.getLogger("django_debug")
warn_logger = logging.getLogger("django_warn")
crit_logger = logging.getLogger("django_crit")
